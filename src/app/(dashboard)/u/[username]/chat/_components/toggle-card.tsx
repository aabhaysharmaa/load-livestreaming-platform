
interface ToggleCardProps {
	field: string
	label: string
	value: boolean
}

const ToggleCard = ({
	field,
	label,
	value
}: ToggleCardProps) => {
	return (
		<div>

		</div>
	)
}

export default ToggleCard
